# This folder includes the necessary files to train a TS-DAR model on HPC systems:

(1) train.py - contains the code to load the systems data and train the TS-DAR model
(2) submit.sh - the submit file (type "sbatch submit.sh" to submit on HPC systems)


NOTE: create conda environment with dependencies of python and python packages:
python == 3.9
numpy == 1.26.1
scipy == 1.11.4
torch == 1.13.1
tqdm == 4.66.1

To install the source code:

git clone https://github.com/xuhuihuang/ts-dar.git
python -m pip install ./ts-dar

You can get more instructions from: https://github.com/xuhuihuang/ts-dar?tab=readme-ov-file
